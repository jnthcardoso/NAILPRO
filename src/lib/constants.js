// Ciclo de retorno padrão (em dias) usado quando a cliente não tem um
// valor próprio definido (clientes.dias_retorno = NULL).
export const DIAS_RETORNO_PADRAO = 30

// A partir de quantas visitas a cliente é considerada VIP.
export const VISITAS_VIP = 10
